# MST

Build the MST example with:

```sh
make
```

Expected output (first build):

```text
g++ -std=c++20 -g -Wall prims.cc -o prims
```

Run the program with:

```sh
./prims
```

Expected output:

```text
2 3 6 5 
Total: 16
```
